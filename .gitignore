# 本仓是 RuoYi-Vue-Plus 的「前后端一体」组织方式：
#   backend/  —— 后端，源自 dromara/RuoYi-Vue-Plus
#   frontend/ —— 前端，源自 JavaLionLi/plus-ui
# 两个子目录各自保留上游的 .gitignore（target/、node_modules/、dist/、data/ 等
# 都由它们负责），这里只处理仓库根一级的杂物。

*.log
logs/

# 编辑器 / 系统
.idea/
.vscode/
.DS_Store
Thumbs.db
